# Фаза 3 — Настройка сервера

## Статус
- Статус: ✅
- Дата/время: 2026-04-07 07:43:46 +05:00

## Цель фазы
- Подготовить сервер к клонированию проекта и установке Python-зависимостей.

## Что сделано
- Выполнен `apt update`.
- Установлены или подтверждены пакеты `python3`, `python3-venv`, `python3-pip`, `git`.
- Сняты версии бинарников и пакетов.

## Проблемы и отклонения
- Существенных проблем в этой фазе не возникло.

## Как решено
- Фаза выполнена штатно через SSH alias `voice-inbox-server`.

## Проверки
- `python3 --version` → `Python 3.12.3`
- `python3 -m venv --help` отрабатывает
- `python3 -m pip --version` → `pip 24.0`
- `git --version` → `git version 2.43.0`
- `dpkg -s python3-venv python3-pip git` подтвердил статус `install ok installed`

## Артефакты и изменения
- На сервер установлены необходимые системные пакеты для Python-сервиса и deploy.

## Следующий шаг
- Перейти к Фазе 4: создать deploy key на сервере и дать серверу доступ к приватному GitHub-репозиторию.
